import { ReactElement, ReactNode } from "react";

/**
 * Only use JSX containing pdf-* elements
 */
export type PdfNode = ReactNode;

/**
 * Only use pdf-* elements
 */
export type PdfElement = ReactElement;
